---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kubepage-operator
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: kubepage-operator
  interval: 1h
  path: ./kubernetes/apps/default/kubepage-operator/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: OCIRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  wait: true
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kubepage-config
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: kubepage-operator
  interval: 1h
  prune: true
  sourceRef:
    kind: OCIRepository
    name: kubepage-config
  targetNamespace: default
  wait: false
